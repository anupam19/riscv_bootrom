/* BootROM configuration: address of the payload to jump to.
   This symbol is defined here and referenced by boot_main.c.
   Adjust if the payload load address changes. */
const unsigned long payload_addr = 0x80020000UL;
